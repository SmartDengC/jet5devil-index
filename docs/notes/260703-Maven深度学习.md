

简单来深度了解一下maven的相关知识点



## Maven Setting.conf

我们可以看到配置文件最外层是一个settings的标签，里面有很多的子标签都是呈现主子表现出来的。

localRepository 表示下载下来的包存放在哪里

pluginGroups

- servers 
  - server
- mirrors
  - mirror

```xml
   <mirrors>
       <mirror>
           <id>central</id>
           <name>DTHost003-releases</name>
           <mirrorOf>central</mirrorOf>
           <url>http://62.234.217.203:50006/artifactory/libs-release</url>
       </mirror>
       <mirror>
           <id>alimaven</id>
           <mirrorOf>resp1</mirrorOf>
           <name>aliyun maven</name>
           <url>http://maven.aliyun.com/nexus/content/groups/public/</url>
       </mirror>
   </mirrors>
```



- profiles
  - profile

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
   <localRepository>/Users/dengc4r/.m2/repository</localRepository>
   <pluginGroups></pluginGroups>
   <proxies></proxies>
   <servers>
       <server>
           <id>releases</id>
           <username>admin</username>
           <password>admin</password>
       </server>
       <server>
           <id>snapshots</id>
           <username>admin</username>
           <password>admin</password>
       </server>
   </servers>
   <mirrors>
       <mirror>
           <id>central</id>
           <name>DTHost003-releases</name>
           <mirrorOf>central</mirrorOf>
           <url>http: //62.234.217.203:50006/artifactory/libs-release</url>
       </mirror>
       <mirror>
           <id>alimaven</id>
           <mirrorOf>resp1</mirrorOf>
           <name>aliyun maven</name>
           <url>http: //maven.aliyun.com/nexus/content/groups/public/</url>
       </mirror>
   </mirrors>
   <profiles>
       <profile>
           <id>myprofile</id>
           <repositories>
               <repository>
                   <id>central</id>
                   <url>http: //62.234.217.203:50006/artifactory/libs-release/</url>
                   <releases>
                       <enabled>true</enabled>
                   </releases>
                   <snapshots>
                       <enabled>true</enabled>
                   </snapshots>
               </repository>
           </repositories>
           <pluginRepositories>
               <pluginRepository>
                   <id>central</id>
                   <url>http: //62.234.217.203:50006/artifactory/libs-release/</url>
                   <releases>
                       <enabled>true</enabled>
                   </releases>
                   <snapshots>
                       <enabled>false</enabled>
                   </snapshots>
               </pluginRepository>
           </pluginRepositories>
       </profile>
   </profiles>
   <activeProfiles>
       <activeProfile>myprofile</activeProfile>
   </activeProfiles>
</settings>
```





## Maven 命令

### 1 常用的打包命令

```
mvn clean package -Dmaven.test.skip=true -- 跳过单元测试打包
mvn clean install -Dmaven.test.skip=true -- 跳过单元测试打包，并把大好的包上传到本地仓库

```

Maven 命令的格式位mvn [plguin-name]:[goal-name]

-D 指定参数， 如-Dmaven.test.skip=true跳过单元测试

-P指定profile配置，可以用于区分环境

-e 显示maven运行出错的信息

-o 离线执行命令，即不去远程仓库更新包

-X 显示maven运行的debug信息

-U 强制去远程更新snapshot的插件或依赖，默认每天更新一次。



maven打包：mvn package

mvn clean 清楚target里面编译出来的内容

mvn compile 编译

mvn test-compile 

mvn test 运行测试类



mvn clean test 如果想要一次性执行多个命令的话，可以写在一行上

mvn install 把jar包或者是war安装到本地仓库，简单来理解就是，一个项目里面有多个module， 在A module中引用B module 的打出来的包。



打jar包和war包jdk插件是有区别的，我们在打war包的时候，需要手动添加一个插件

```
<plugins>
	<plugin>
		<groupId>org.apache.maven.plugins</groupId>
		<artifactId>maven-war-plugin</artifactId>
		<version>3.2.2</version>
	</plugin>
</plugins>
```

