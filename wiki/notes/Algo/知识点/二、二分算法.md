---
title: 二分算法（二）
author: 邓聪的小破站
createTime: 2024/02/21 22:41:05
permalink: /algo/665kj8fq/
outline: [2, 4]
tags:
  - algo
  - 二分查找
---

视频讲解：[二分查找 红蓝染色法【基础算法精讲 04】](https://www.bilibili.com/video/BV1AP41137w7/?vd_source=35e7dde81183ac464990a0a0ab794bce)

灵神提单：[二分算法（二分答案/最小化最大值/最大化最小值/第K小）](https://leetcode.cn/circle/discuss/SqopEo/)

## 一、二分查找

### 1.1、基础

#### [34. 在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)（20250401更新）

题目：

>给你一个按照非递减顺序排列的整数数组 `nums`，和一个目标值 `target`。请你找出给定目标值在数组中的开始位置和结束位置。
>
>如果数组中不存在目标值 `target`，返回 `[-1, -1]`。
>
>你必须设计并实现时间复杂度为 `O(log n)` 的算法解决此问题。



\>=, <= 都可以转化用 binarySearch 来转化

比如\>=x 直接用， <=x 可以转化成>= x+1, 最后取下标减一。

:::code-tabs

@tab 两端闭区间[left, right]

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int start = binarySearch(nums, target);
        if (start == nums.length || nums[start] != target) {
            return new int[] { -1, -1 };
        }
        int end = binarySearch(nums, target + 1) - 1;
        return new int[] { start, end };
    }

    int binarySearch(int[] nums, int target) {
        // 两端闭区间的情况[left, right]
      	// 两端闭区间的时候，left移动到mid+1， right移动到mid-1
        int left = 0;
        int right = nums.length - 1;

      	// 区别地方：两端闭，当left=right的时候也需要处理
        while (left <= right) { 
            int mid = left + (right - left) / 2;
            if (nums[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        return left;
    }
}
```

@tab  左闭右开区间[left, right)

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int start = binarySearch(nums, target);
        if (start == nums.length || nums[start] != target) {
            return new int[] { -1, -1 };
        }
        int end = binarySearch(nums, target + 1) - 1;
        return new int[] { start, end };
    }

    int binarySearch(int[] nums, int target) {
        // 左闭右开区间[left, right)
        // 左闭右开的时候，右节点在移动时就移动到mid的位置
        int left = 0, right = nums.length;
      
      	// 区别地方，左闭右开因为取不到右侧，所以最后一个点就是left+1=right，在移动就跳出循环了
        while (left < right) {
            int mid = left + (right - left) / 2;

            if (nums[mid] < target) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }
        return left;
    }
}
```

@tab 两端开区间(left, right )

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int start = binarySearch(nums, target);
        if (start == nums.length || nums[start] != target) {
            return new int[] { -1, -1 };
        }
        int end = binarySearch(nums, target + 1) - 1;
        return new int[] { start, end };
    }

    int binarySearch(int[] nums, int target) {
        // 两端开区间(left, right)
        // 两端开区间，那么left移动到mid， right移动到mid
        int left = -1, right = nums.length;
      	// 区别地方，两端开区间相对于左闭右开，左指针左移了一位。
        while (left + 1 < right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] < target) {
                left = mid;
            } else {
                right = mid;
            }
        }
        return right;
    }
}
```

:::

#### [704. 二分查找](https://leetcode.cn/problems/binary-search/)（20250401更新）

二分查找最基础的题，直接套用框架求解，都不需要做额外的操作。

